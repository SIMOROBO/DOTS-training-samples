#ifndef ParticleCommonIncluded
#include "CommonParticleComputeShader.compute"
#define ParticleCommonIncluded
#endif


SimOutput RunSim0(int index)
{
  SimOutput simOutput;
  simOutput.normal = float3(0,1,0);
  simOutput.dist = 1;
  return simOutput;
}
